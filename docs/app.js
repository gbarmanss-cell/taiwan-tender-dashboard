const state={items:[],searchDate:""};const keywordInput=document.querySelector("#q");const typeSelect=document.querySelector("#type");const searchButton=document.querySelector("#searchButton");const list=document.querySelector("#list");const count=document.querySelector("#count");const updated=document.querySelector("#updated");const dateLabel=document.querySelector("#dateLabel");const money=value=>value==null?"未提供預算":new Intl.NumberFormat("zh-TW",{style:"currency",currency:"TWD",maximumFractionDigits:0}).format(value);const safe=value=>String(value??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]));function render(){const keyword=keywordInput.value.trim().toLowerCase();const selectedType=typeSelect.value;const rows=state.items.filter(item=>{const haystack=`${item.title} ${item.agency} ${item.category}`.toLowerCase();return(!keyword||haystack.includes(keyword))&&(!selectedType||item.category===selectedType)});count.textContent=`找到 ${rows.length} 筆`;dateLabel.textContent=state.searchDate?`｜公告日期：${state.searchDate}`:"";if(!rows.length){const details=[selectedType?`${selectedType}案`:"全部類型",keyword?`關鍵字「${keywordInput.value.trim()}」`:"未指定關鍵字"].join("、");list.innerHTML=`<div class="empty">今日沒有符合「${safe(details)}」的標案。</div>`;return}list.innerHTML=rows.map(item=>`<article class="card"><div class="type-badge">${safe(item.category)}案</div><h2><a href="${safe(item.source_url)}" target="_blank" rel="noopener noreferrer">${safe(item.title)}</a></h2><div class="meta"><span>${safe(item.agency||"未標示機關")}</span><span>公告：${safe(item.notice_date||"—")}</span><span>截止：${safe(item.deadline||"—")}</span><span class="budget">${safe(money(item.budget))}</span></div></article>`).join("")}function search(){render();keywordInput.focus()}searchButton.addEventListener("click",search);typeSelect.addEventListener("change",render);keywordInput.addEventListener("keydown",event=>{if(event.key==="Enter")search()});fetch("data/tenders.json",{cache:"no-store"}).then(response=>{if(!response.ok)throw new Error(`HTTP ${response.status}`);return response.json()}).then(data=>{state.items=(data.items||[]).filter(item=>["勞務","財物","工程"].includes(item.category));state.searchDate=data.search_date||"";updated.textContent=data.generated_at?`資料更新：${new Date(data.generated_at).toLocaleString("zh-TW")}`:"";render()}).catch(error=>{count.textContent="資料讀取失敗";list.innerHTML=`<div class="empty">尚未產生今日資料。請先執行 crawler.py 與 export_json.py。<br>${safe(error.message)}</div>`});
